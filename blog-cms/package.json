{
  "name": "forward-blog-cms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute forward_blog --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute forward_blog --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20250101.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.0.0"
  }
}
